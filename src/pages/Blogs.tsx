import React from 'react';
import styles from './Blogs.module.css';

const Blogs: React.FC = () => {
  return (
    <div className={styles.blogsPage}>
      <main className={styles.mainContent}>
        {/* Blogs page content will go here */}
      </main>
    </div>
  );
};

export default Blogs;
